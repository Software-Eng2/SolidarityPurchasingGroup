import{ Container, Row, Col } from "react-bootstrap";

function OrderPage(props){

    return (
        <Container fluid>
            ORDERS {/* TO DO: implemet order list */}
        </Container>
    );
}

export default OrderPage;

